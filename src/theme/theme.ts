import { fonts } from 'src/theme/setting/fonts';
import { zindex } from 'src/theme/setting/zindex';
import { colors } from 'src/theme/setting/colors';
import { breakpoint } from 'src/theme/setting/breakpoint';

export const theme = {
  breakpoint: breakpoint,
  colors: colors,
  zindex: zindex,
  fonts: fonts,
};